<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="True"
             Shell.TabBarIsVisible="False"
             Title="Login"
             x:Class="MobileUi.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout Margin="20,40">
            <Frame HasShadow="False">
                <Label Text="Welcome to Feed Reader" HorizontalTextAlignment="Center" FontSize="Title"/>
            </Frame>
            <Entry Text="{Binding Username}" Placeholder="Username"/>
            <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True"/>
            <Button Text="Login" Command="{Binding ExecuteLogin}" Margin="0,20"/>
            <Label Text="Register" HorizontalOptions="Center" TextColor="Accent" TextDecorations="Underline">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ExecuteRegister}" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>