<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileUi.Views.FeedsPage">
    <ContentPage.Content>

        <StackLayout Spacing="0">
            <Label Text="Feed channels"  Background="Blue" TextColor="White" Padding="10,10"/>
            <CollectionView ItemsSource="{Binding FeedChannels}" Background="#dddddd">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Spacing="0" Padding="20,10,0,0">
                            <Label Text="{Binding Title}" VerticalTextAlignment="Center" 
                                   FontAttributes="Bold"/>
                            <Label Text="{Binding Description}" VerticalTextAlignment="Center" 
                                   FontAttributes="Italic"/>

                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>

    </ContentPage.Content>
</ContentPage>